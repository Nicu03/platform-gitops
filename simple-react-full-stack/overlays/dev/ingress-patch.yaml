apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-react-full-stack
spec:
  tls:
    - hosts:
        - react.dev.local
      secretName: react-dev-local-tls
  rules:
    - host: react.dev.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: simple-react-full-stack
                port:
                  number: 80
